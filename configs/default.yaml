model:
  name: google/gemma-2-9b
  dtype: auto
  device: auto

sae:
  layer: 20
  d_sae: 16384
  top_k: 10
  checkpoint: null

jlens:
  layers: null  # null = auto-sample across depth
  top_k: 5

patching:
  layers: null  # null = all layers
  metric: default

data:
  prompts_path: data/prompts/examples.json

results:
  root: results
